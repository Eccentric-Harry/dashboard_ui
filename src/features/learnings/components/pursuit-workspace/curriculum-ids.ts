// DOM ids shared by the curriculum, the milestone navigator and the scroll spy.

export const sectionDomId = (stepId: string) => `pw-section-${stepId}`

export const stepBodyId = (stepId: string) => `pw-steps-${stepId}`
