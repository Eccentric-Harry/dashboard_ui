// Calendar domain types. Re-exported from lib/api during migration; Phase-B
// cleanup moves the definitions here.

export type {
  CalendarItem,
  CalendarItemPayload,
  CalendarItemType,
  CalendarRecurrence,
  GoogleCalendarAccount,
  GoogleSyncStatus,
} from '../lib/api';
